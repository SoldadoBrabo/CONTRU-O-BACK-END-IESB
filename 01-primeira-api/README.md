# Iniciar projeto
npm init

# Instalar bibliotecas

npm install express
npm install nodemon

{
  "name": "01-api-inicial",
  ...
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  ...
}